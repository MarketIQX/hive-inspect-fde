# Copy to .env.local for local development, or set this value directly in
# your hosting environment. The application uses only a server-side Postgres
# connection string; no browser-side Supabase key is required.
DATABASE_URL=
